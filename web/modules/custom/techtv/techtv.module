<?php

use Drupal\Core\Form\FormStateInterface;

/**
 * Implements hook_form_alter().
 */
function techtv_form_alter(&$form, FormStateInterface $form_state, $form_id) {
  if ($form_id == 'node_tv_edit_form' || $form_id == 'node_tv_form') {
//    dd($form_state->getUserInput());
  }
}
